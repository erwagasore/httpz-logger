# httpz-logger

TODO: add project description

## Quickstart

```bash
git clone git@github.com:erwagasore/httpz-logger.git
cd httpz-logger
```

TODO: add build/run instructions (no build system detected yet — see `MIGRATION_PLAN.md` for planned Zig setup)

## Structure

See [AGENTS.md](AGENTS.md#repo-map) for the full repo map.
